{% extends 'base.html.twig' %}

{% block body %}
    <h1>Fruit creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-primary"/>
        <a href="{{ path('fruit_homepage') }}" class="btn btn-info">Back to the list</a>
    {{ form_end(form) }}



{% endblock %}
