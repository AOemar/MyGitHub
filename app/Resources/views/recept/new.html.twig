{% extends 'base.html.twig' %}

{% block body %}
    <h1>Recept creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-primary"/>
    {{ form_end(form) }}

        <a href="{{ path('recept_index') }}">Back to the list</a>

{% endblock %}
